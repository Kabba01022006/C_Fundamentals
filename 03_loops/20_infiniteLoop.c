// infinite loop is a loop code which never ends.

#include<stdio.h>
int main()
{
    int i=1;
    while(i<10)
    {
        printf("%d\n",i);
        i--;
    }
}

// this loop is an infinite loop and will never end. 