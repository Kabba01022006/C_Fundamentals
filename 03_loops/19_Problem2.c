#include<stdio.h>
int main()
{
    //ascii value of 'a' is 97.
    //ascii value of 'b' is 98.
    while ('a'<'b')
    printf("\nmalayalam is a palindrome");
    // 97 is always smaller than 98 , therefore this is an infinite loop.
} 
