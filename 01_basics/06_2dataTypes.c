// int  -- integer -- %d(format specifier) -4bytes   
// short -- alsoAnInteger -- %d(format specifier) but thing is that it has a range of -32768 to +32767.
//short - 2bytes
// float -- realnumber(decimals) -- %f (format specifier)
// char -- charachter(letter) -- %c (format specifier)
// char ch = 'a'; Single commas mein a-z , A-Z , !@#$%^&()... etc aa sakte h.

//  ******** if i have n bits , it can store 2**n numbers.

// long - 8bytes
// long long 

